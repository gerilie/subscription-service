root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/app cmd/subscription/*.go"
entrypoint = "tmp/app"
include_ext = ["go"]
exclude_dir = ["tmp", "vendor", "docs"]
delay = 500

[log]
time = true

[color]
main = "yellow"
watcher = "cyan"
build = "green"
runner = "magenta"
